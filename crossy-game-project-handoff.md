# 2D 過馬路網頁遊戲：專案交接說明

## 給新聊天室的指示

請協助我規劃並逐步實作以下專案。除非我明確要求一次完成，否則請將工作拆成小步驟，每一步說明目的、需要建立或修改的 Unity 物件／檔案，以及如何在 Unity Editor 中驗證。請優先完成可玩的最小版本，再逐步加入後端，不要一開始建立過度複雜的架構。

## 我的背景與主要目的

- 我曾經用 C# 和 Unity 做過一款類似《吸血鬼倖存者》的電腦單機遊戲。
- 這次最重要的目的是練習後端技術，並完成一個可用於遊戲工程師面試的作品集專案。
- 遊戲不需要 3A 規模，小而完整、可公開遊玩、能清楚展示工程能力比較重要。
- 成品希望可以直接在網頁瀏覽器中遊玩。
- 遊戲客戶端繼續使用 Unity 與 C#，後端也優先使用 C# 技術棧。

## 遊戲概念

製作一款受到 Crossy Road 啟發、但具有自己名稱與美術風格的 2D 無限過馬路遊戲。

核心玩法：

- 2D 俯視角或略帶斜視感的正交視角。
- 玩家以格子為單位向上、下、左、右移動。
- 地圖由一條條橫向 Lane 組成。
- Lane 類型包括草地、馬路，後續可擴充河流與鐵路。
- 汽車在道路上向左或向右移動。
- 玩家碰到汽車死亡。
- 玩家向前走得越遠，分數越高。
- 前方持續生成新 Lane，後方太遠的 Lane 會被回收。

這不是要完全複製 Crossy Road。正式作品需要使用自己的遊戲名稱、角色、美術、音效及部分差異化玩法。

## 已決定的 Unity 設定

- Unity 版本：Unity 6 `6000.3.10f1 LTS`。
- 專案範本：`Universal 2D`。
- 使用 C#。
- 使用 `Rigidbody2D`、`Collider2D`、Sprite，必要時使用 Tilemap。
- 最終輸出目標：Unity Web Build。
- 需要在 Unity Hub 為這個 Editor 安裝 Web Build Support。
- 不使用 Unity Version Control，改用 Git／GitHub。

## 遊戲客戶端預計架構

建議保持資料驅動並避免過早複雜化，可能包含以下元件：

- `PlayerController`：讀取輸入、格子移動、死亡狀態。
- `Lane`：代表單一橫向區域。
- `LaneConfig`：Lane 類型、車輛方向、速度、間距及可用障礙物。
- `LaneGenerator`：根據 Seed／難度生成前方 Lane。
- `LaneRecycler`：回收玩家後方太遠的 Lane。
- `Vehicle`：沿固定方向、固定速度移動，不需要尋路 AI。
- `VehicleSpawner`：依照最小／最大距離或時間生成車輛。
- `ObjectPool`：回收汽車、障礙物和 Lane，避免反覆 Instantiate／Destroy。
- `GameManager`：管理 Playing、Dead、GameOver 等狀態。
- `ScoreManager`：以前進最遠距離計分，不能藉由後退重複加分。
- `CameraFollow`：相機隨玩家向前，但通常不向後退。

## 汽車與障礙物邏輯

汽車：

- 只沿 Lane 的 X 軸左右移動。
- 每條 Lane 有方向、速度、車種、最小和最大間距。
- 車輛離開邊界後回到物件池。
- 車輛之間不需要真實物理碰撞，避免互撞或阻塞。
- 使用 Kinematic `Rigidbody2D` 和 Trigger Collider 較容易控制。
- Spawner 必須確保出生點沒有被占用，或以與上一台車的距離決定何時生成，避免重疊。

固定障礙物：

- 例如樹、石頭、柵欄，阻止玩家移入該格。
- 程序生成時不能封死整條前進路徑。
- 初期可以使用預先設計的 Lane Pattern 控制公平性；後期再考慮 BFS／Flood Fill 驗證可通行性。

## 難度設計

不要只讓汽車速度無限增加。難度可由以下因素組合：

- 連續危險 Lane 數量。
- 車輛速度，但要有上限。
- 車輛間距，但要保留合理反應時間。
- 不同方向、不同速度的相鄰 Lane。
- 汽車、卡車、機車等不同長度和速度。
- 安全草地出現機率。
- 後期才加入火車或河流移動平台。

生成規則至少需要保證：

- 開局數條 Lane 安全。
- 不能產生完全封閉的固定障礙物。
- 連續危險 Lane 有上限。
- 高速危險物有足夠警示或可辨識空檔。
- 不在玩家所在位置生成危險物。

## 建議的第一版 MVP

第一版只做：

1. 一張固定測試地圖。
2. 玩家四方向格子移動。
3. 草地與馬路兩種 Lane。
4. 一種汽車左右移動。
5. 撞擊死亡與重新開始。
6. 根據前進最遠距離計分。
7. 接著才加入 Lane 無限生成及回收。
8. 接著加入汽車物件池。

第一版暫時不做：

- 即時多人遊戲。
- 河流與浮木。
- 火車。
- 商店、角色抽取或大量造型。
- 複雜任務系統。
- 大量關卡內容。

## 後端目標

遊戲可玩版本完成後，建立：

- ASP.NET Core Web API。
- PostgreSQL。
- Entity Framework Core。
- ASP.NET Core Identity 或適合 API 的標準認證機制。
- Razor Pages 或 Blazor Server 管理後台。
- OpenAPI／Swagger。
- xUnit 測試。
- Docker 部署。

後端功能分階段完成：

### 第一階段

- 建立匿名訪客身分。
- 後端核發 `PlayerId` 與 Access Token。
- 設定玩家顯示名稱。
- 建立遊戲局 `RunId`。
- 提交及保存遊戲結果。
- 每日排行榜。
- 個人最佳分數及歷史紀錄。

### 第二階段

- 使用者名稱或 Email＋密碼的正式註冊、登入與登出。
- 允許訪客升級成正式帳號，保留原有紀錄。
- 跨裝置保存。
- 一般玩家及管理員 Role。
- 管理後台查看玩家、遊戲紀錄與異常成績。
- 停權玩家或隱藏無效成績。

### 第三階段

- 每日挑戰。
- 後端每天產生固定 Seed 與遊戲規則。
- 當天所有玩家使用相同 Seed 生成相同或可重現的 Lane 序列。
- 遠端遊戲設定，例如速度倍率、可用 Lane 類型與活動規則。
- 遊戲版本檢查、Rate Limit、結構化 Log。

## 排行榜與基本防作弊

排行榜不一定要求玩家先註冊，但每筆成績必須對應由後端核發的穩定 `PlayerId`，不能只相信玩家輸入的暱稱。

建議流程：

1. Unity 向後端要求開始一局。
2. 後端建立一次性 `RunId`，回傳每日 Seed 和規則。
3. 玩家完成或死亡後，Unity 提交 `RunId`、分數、遊戲時間、Seed 及遊戲版本。
4. 後端驗證該 `RunId` 存在、尚未提交、時間和分數在合理範圍內。
5. 通過後才寫入有效成績並更新排行榜。

後端不能完全相信 Unity 傳來的分數。第一版不必做到商業級防作弊，但至少需要：

- `RunId` 只能完成一次。
- 驗證開始和結束時間。
- 驗證分數增長上限。
- 驗證每日 Seed 與遊戲版本。
- 可以把可疑紀錄標示為無效。

## 建議資料表概念

- `Players`：遊戲內玩家身分、顯示名稱、是否訪客、是否停權。
- Identity 使用者相關資料表：正式帳號、密碼雜湊、Role。
- `DailyChallenges`：日期、Seed、規則、版本。
- `GameRuns`：PlayerId、RunId、分數、開始時間、結束時間、版本、是否有效。

顯示名稱可以修改，但資料關聯必須使用不可變的 `PlayerId`。

## 預計公開部署

初期免費部署方案：

- Unity Web 遊戲：itch.io。
- ASP.NET Core API 與管理後台：Render，完成後可遷移到 Google Cloud Run 練習正式雲端部署。
- PostgreSQL：Neon Free。
- 原始碼及 CI/CD：GitHub／GitHub Actions。

Render 免費 API 閒置時可能休眠，因此第一次請求可能較慢。不要使用會短期到期的免費 Render PostgreSQL，資料庫改用 Neon。

Unity 遊戲在 itch.io、API 在另一個網域，因此需要：

- 所有 API 使用 HTTPS。
- 正確設定 ASP.NET Core CORS，只允許實際遊戲來源。
- API 使用 Bearer Access Token；不要依賴可能被第三方 Cookie 政策阻擋的跨網站 Cookie。
- 資料庫 Connection String、JWT Key 等 Secret 只能放在伺服器環境變數，不能放入 Unity Build 或 GitHub。

## Git 與作品集要求

- Unity 專案使用適合 Unity 的 `.gitignore`，不要提交 `Library`、`Temp`、`Logs`、`obj` 等產物。
- 客戶端與後端可以先放在同一個 repository 的不同資料夾，方便展示完整架構。
- README 最後應包含：遊戲網址、測試帳號、短影片、架構圖、執行方式、API／資料庫說明、驗證與防作弊設計、測試方式及技術取捨。
- 優先保持可執行、可測試的 Commit，每完成一個小功能就驗證。

## 新聊天室應從這裡開始

請先檢查目前 Unity 專案的實際檔案結構與 Unity 版本，不要假設場景或檔案已存在。接著協助我完成第一個最小里程碑：

> 在固定場景中建立一個暫用方塊／Sprite 玩家，使玩家能以方向鍵或 WASD 一次移動一格，移動過程有短暫平滑動畫，且移動期間不能重複接受輸入。

完成後請告訴我：

- 建立或修改了哪些檔案。
- Unity Editor 中需要如何掛載元件。
- 如何進入 Play Mode 驗證。
- 下一個最小步驟是什麼。
